Put your assets here:

  resume.pdf   → the "Download resume" button in the hero links to assets/resume.pdf
  images/      → project thumbnails, profile photo, favicon, etc.

Use RELATIVE paths in HTML/CSS (e.g. assets/img/photo.jpg) so they keep working
after deploying to Vercel. Absolute local paths (C:\Users\...) will break online.
